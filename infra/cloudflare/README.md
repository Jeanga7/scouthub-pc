# Cloudflare Pilot Notes

The pilot target is Cloudflare Workers through OpenNext.

`wrangler.jsonc` declares the expected bindings for R2, Queues and Cron, but this repository does not create remote Cloudflare resources automatically. Create or bind production resources only after Product Owner approval and cost review.
